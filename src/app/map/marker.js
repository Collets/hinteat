/** MarkerInfo class */
export default class MarkerInfo {
  /**
   * Constructor of class MarkerInfo
   * @param {string} title
   * @param {Object} position
   * @param {string} url
   */
  constructor(title, position, url) {
    this.title = title;
    this.position = position;
    this.url = url;
  }
}
